import { Chat } from "@/components/chat/chat";
import { ChatLayout } from "@/components/chat/chat-layout";
import React from "react";

const page = () => {
  return <ChatLayout defaultLayout={undefined} navCollapsedSize={8} />;
};

export default page;
